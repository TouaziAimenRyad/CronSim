include "../include/included.h"
include "../include/command.h"

/*
char * * getwords(char * str)             //needs fixing  and checking 
{ 
    char *  words[5];
    int i=0;
	int init_size = strlen(str);
	char delim[] = " ";

	char *ptr = strtok(str, delim);

	while(ptr != NULL)
	{
		words[i]=ptr;
		ptr = strtok(NULL, delim);
        i++;
	}

	return words;


}


uint32_t convert_str_to_uint32(char *str) //needs fixing and checking 
{
    uint32_t value = strtoul(cmd.c_str(), NULL, 10);
    return value;

}

uint32_t * convert_str_to_ARGV(char * str){


}*/